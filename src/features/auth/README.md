# Auth feature

This directory holds auth-specific hooks, forms, and services beyond the
generic scaffolding in `src/services/firebase/auth.service.ts` and
`src/context/AuthContext.tsx`.

Add role assignment, invitation flows, MFA, and profile bootstrap logic here
as they are built out.
